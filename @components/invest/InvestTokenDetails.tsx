import React, { ReactElement } from 'react';

// eslint-disable-next-line @typescript-eslint/no-empty-interface
interface InvestTokenDetailsProps {}

export function InvestTokenDetails({}: InvestTokenDetailsProps): ReactElement {
  return (
    <div>
      <p>This not done yet. Depends on implementking KYA storage on IPFS</p>
    </div>
  );
}
