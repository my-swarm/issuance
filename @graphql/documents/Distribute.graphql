query Distrubute($id: ID!) {
  token(id: $id) {
    id
    address
    availableSupply
    stake
    currentFundraiser {
      contributors {
        address
        amount
        status
      }
    }
  }
}
