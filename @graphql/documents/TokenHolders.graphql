fragment TokenHolder on TokenHolder {
  address
  balance
  createdAt
  updatedAt
  isFrozen
}

query TokenHolders($token: ID!) {
  token(id: $token) {
    id
    features {
      accountBurn
      accountFreeze
      forceTransfer
    }
    holders {
      ...TokenHolder
    },
  }
}
