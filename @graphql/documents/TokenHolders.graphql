fragment TokenHolder on TokenHolder {
  address
  balance
  createdAt
  updatedAt
  isFrozen
}

query TokenHolders($token: String!) {
  tokenHolders(where: { token: $token }) {
    ...TokenHolder
  },
}
