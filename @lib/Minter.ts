import { DeployerState } from '@types';
import { getContractArtifacts } from '@lib/ContractArtifacts';
import { ContractProxy } from '@lib/ContractProxy';

export class Minter {
  public constructor(contractProxy: ContractProxy) {
    this.contractProxy = contractProxy;
  }

  public async;
}
